# Simplex Widget

Start backend project:

    cd backend
    cp .env.exampe .env
    npm install
    npm start

Start frontend project:

    cd frontend
    npm install
    npm start
    open http://localhost:8080

